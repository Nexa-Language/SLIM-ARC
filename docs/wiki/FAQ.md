# FAQ

## 为什么仓库没有模型？

80B GGUF 超过 GitHub 文件限制，且模型有独立许可证。仓库提供版本和哈希，不分发权重。

## 为什么不同页面的 token/s 不同？

设备、量化、缓存、prompt、`pp/tg` 和资源约束不同。只应比较同一实验合同内的结果。

## bootstrap 会修改系统吗？

不会。它只在指定 `SLIM_ARC_LLAMA_ROOT` 下克隆固定上游版本、应用 patch 并生成构建目录。

## 是否还能继续开发？

可以。仓库不归档，欢迎平台适配、复现修正和论文补充。
